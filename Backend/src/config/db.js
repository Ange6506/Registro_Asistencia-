const CONFIG_DB = {
  user: "postgres",
  host: "localhost",
  database: "Register_Asistencia",
  password: "Admin",
  port: 5432, // Puerto por defecto de PostgreSQL
};

module.exports = {
  CONFIG_DB,
};
