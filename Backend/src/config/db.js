const CONFIG_DB = {
  user: "postgres",
  host: "localhost",
  database: "registrar_asistencia",
  password: "Admin",
  port: 5432, // Puerto por defecto de PostgreSQL
};

module.exports = {
  CONFIG_DB,
};
