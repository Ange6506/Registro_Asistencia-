const CONFIG_DB = {
  user: "postgres",
  host: "localhost",
  database: "RegisterAsistencia",
  password: "Admin",
  port: 5432, // Puerto por defecto de PostgreSQL
};

module.exports = {
  CONFIG_DB,
};
